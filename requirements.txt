# Versions checked against PyPI on 2026-07-15, not recalled. The first cut of this
# file was pinned from memory and shipped anthropic==0.40.0, which predates both
# `thinking` and `output_config` -- the worker died with
# "Messages.create() got an unexpected keyword argument 'thinking'".

# 2.2.0 (2026-06-17). Verified: on 1.17.x the Prometheus middleware is on by default
# and binds an HTTP port per worker process. None of the 2.0.0 breaks touch the
# Retries/WindowRateLimiter APIs used here.
dramatiq[redis]==2.2.0

fastapi==0.139.0
uvicorn[standard]==0.51.0
pydantic==2.13.4

psycopg[binary,pool]==3.3.4
pgvector==0.5.0
redis==8.0.1

httpx==0.28.1
# Mandatory for Quora, not a preference. Measured from one IP: httpx (OpenSSL TLS)
# was challenged 0/6; curl_cffi (Chrome TLS) succeeded 3/3. Quora fingerprints the
# handshake, so no proxy fixes what this fixes. LinkedIn does not need it.
curl_cffi==0.15.0

# 0.116.0: needs to be recent enough for adaptive thinking + output_config
# (structured outputs). Do not downgrade without re-reading llm/client.py.
anthropic==0.116.0
voyageai==0.5.0

numpy==2.5.1
boto3==1.43.48

pytest==9.1.1
respx==0.23.1
