# Versions checked against PyPI on 2026-07-15, not recalled. The first cut of this
# file was pinned from memory and shipped anthropic==0.40.0, which predates both
# `thinking` and `output_config` -- the worker died with
# "Messages.create() got an unexpected keyword argument 'thinking'".

# 2.2.0 (2026-06-17). Verified: on 1.17.x the Prometheus middleware is on by default
# and binds an HTTP port per worker process. None of the 2.0.0 breaks touch the
# Retries/WindowRateLimiter APIs used here.
dramatiq[redis]==2.2.0

fastapi==0.139.0
uvicorn[standard]==0.51.0
pydantic==2.13.4

psycopg[binary,pool]==3.3.4
pgvector==0.5.0
redis==8.0.1

httpx==0.28.1
# Mandatory for Quora, not a preference. Measured from one IP: httpx (OpenSSL TLS)
# was challenged 0/6; curl_cffi (Chrome TLS) succeeded 3/3. Quora fingerprints the
# handshake, so no proxy fixes what this fixes. LinkedIn does not need it.
# Also carries every Reddit .json fetch, once a cookie jar is held.
curl_cffi==0.15.0

# Reddit cookie jar only -- roughly one browser launch per 2h TTL, never per URL.
# Reddit's gate is state, not fingerprint: with no cookies its .json is 403 (0/6), with
# a browser-minted jar it is 200 (20/20 at 0.83s each) via curl_cffi. The cheap ways to
# mint that jar were measured and both fail -- a curl_cffi GET of the homepage and
# `obscura --stealth --dump cookies` each return a lone `edgebucket`, while headless
# Chromium returns 12 including loid/csv/pxrc, which are written by page JavaScript.
# That is the entire reason this dependency exists; see scrape/reddit.py.
playwright==1.61.0

# Primary LLM + embedding provider. 2.45.0 supports gpt-5-nano's reasoning params
# (max_completion_tokens, reasoning_effort) and strict json_schema structured output.
openai==2.45.0
# Fallback LLM. 0.116.0: recent enough for adaptive thinking + output_config
# (structured outputs). Do not downgrade without re-reading llm/providers.py.
anthropic==0.116.0
# Fallback embeddings.
voyageai==0.5.0

numpy==2.5.1

# Seller side of x402: mints the 402 challenge and calls OKX's broker to verify and
# settle. Not hand-rolled, because the broker contract cannot be inferred -- it needs
# HMAC-SHA256 request signing against web3.okx.com, and a wrong guess fails closed
# forever while looking like a configuration problem. The [evm] extra pulls the
# ethereum packages ExactEvmScheme needs; without it the scheme import raises.
okxweb3-app-x402[evm]==0.1.1

pytest==9.1.1
respx==0.23.1
